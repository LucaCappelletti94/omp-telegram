---
# Excluded from duplication and complexity only, so Opengrep and Trivy still scan tests.
engines:
  duplication:
    exclude_paths:
      - "test/**"
  metric:
    exclude_paths:
      - "test/**"
